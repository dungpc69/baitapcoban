#include<stdio.h>

int main() {
	
	int n, x, a[10000];
	int i;
	scanf("%d%d", &n, &x);
	for (i = 0; i < n; i++) {
		scanf("%d", &a[i]);
	}
	int dem = 0;
	for (i = 0; i < n; i++) {
		if (a[i] == x) {
			dem++;
		}
	}
	if (dem >= 1) {
		printf("YES");
	} else {
		printf("NO");
	}
	
	return 0;
}
