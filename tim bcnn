#include<stdio.h>

int UCLN(int a, int b) {
	if (a % b != 0) {
		return UCLN(b, a % b);
	} else {
		return b;
	}
}

int main() {
	
	int a, b;
	scanf("%d%d", &a, &b);
	int bcnn = a * b / UCLN(a, b);
	printf("%d", bcnn);
	
	return 0;
}
